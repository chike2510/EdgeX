# Shell mobile verification

On 2026-08-16, a 540x1200 Chromium capture of `football.html` was taken from the working tree. The result shows a single compact EdgeX header with hamburger, brand, page context, search/profile actions, one secondary football control row, stable date/tabs/filter controls, no visible fixture jitter, and a single five-item bottom navigation bar. The old full-width sidebar and duplicate mobile menu are not present in the capture.

The Home and landing routes were also captured at 540x1200 after the brand-layer CSS update. Both screenshots completed successfully and were saved under `/tmp/edgex-home-mobile.png` and `/tmp/edgex-landing-mobile.png` for local verification.
